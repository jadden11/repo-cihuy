/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "upload.wikimedia.org",
      "seeklogo.com",
      "cdn-icons-png.flaticon.com",
    ],
  },
};

module.exports = nextConfig;
